#pragma once

#include "BnRTree.h"
#include "HashSet.h"
#include <math.h>
#include <unordered_map>

void algo(HashSet& set, Athlete& user, unordered_map<string, vector<string>> &map);

void algo(BnRTree& tree, Athlete& user, unordered_map<string, vector<string>> &map);